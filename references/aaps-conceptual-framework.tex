\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,fit,calc,backgrounds,shadows,decorations.pathreplacing}

\definecolor{ink}{HTML}{172033}
\definecolor{muted}{HTML}{536071}
\definecolor{paper}{HTML}{FBFDFF}
\definecolor{blue}{HTML}{2F7DF6}
\definecolor{cyan}{HTML}{00A6D6}
\definecolor{green}{HTML}{18A957}
\definecolor{yellow}{HTML}{F7B731}
\definecolor{orange}{HTML}{F06A2F}
\definecolor{pink}{HTML}{DF4E9A}
\definecolor{violet}{HTML}{6F5BD6}
\definecolor{line}{HTML}{9AA8B8}

\tikzset{
  font=\sffamily,
  title/.style={
    font=\sffamily\bfseries\Large,
    text=ink,
    align=center
  },
  subtitle/.style={
    font=\sffamily\small,
    text=muted,
    align=center
  },
  phase/.style={
    draw=#1!70!black,
    fill=#1!10,
    very thick,
    rounded corners=5pt,
    minimum width=3.35cm,
    minimum height=1.36cm,
    align=center,
    text=ink,
    drop shadow={shadow xshift=.8pt,shadow yshift=-.8pt,opacity=.16}
  },
  detail/.style={
    draw=#1!55!black,
    fill=#1!7,
    thick,
    rounded corners=4pt,
    text width=4.35cm,
    align=left,
    inner sep=7pt,
    text=ink
  },
  smallbox/.style={
    draw=#1!55!black,
    fill=#1!8,
    thick,
    rounded corners=4pt,
    text width=3.35cm,
    align=left,
    inner sep=6pt,
    text=ink
  },
  agent/.style={
    draw=#1!70!black,
    fill=#1!13,
    thick,
    rounded corners=12pt,
    text width=3.35cm,
    align=center,
    inner sep=7pt,
    text=ink
  },
  harness/.style={
    draw=#1!60!black,
    fill=#1!4,
    very thick,
    rounded corners=7pt,
    inner sep=8pt
  },
  arrow/.style={
    -{Latex[length=3.2mm,width=2.1mm]},
    very thick,
    draw=line
  },
  feedback/.style={
    -{Latex[length=3mm,width=2mm]},
    thick,
    draw=#1!75!black,
    dashed
  },
  labeltext/.style={
    font=\sffamily\scriptsize,
    text=muted,
    align=center
  },
  note/.style={
    font=\sffamily\scriptsize,
    text=ink,
    align=left
  }
}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \node[title] at (12,9.35) {AAPS Conceptual Framework};
  \node[subtitle, text width=22cm] at (12,8.82)
    {Autonomous Agentic Pipeline Script as a natural-language-first, typed, compilable, executable harness around AI agents.};

  % Main top-down flow.
  \node[phase=blue]   (intent)   at (0,7.55) {\textbf{Scientific intent}\\[-1pt]\scriptsize goals, priors, experiment design};
  \node[phase=cyan]   (studio)   at (4,7.55) {\textbf{AAPS Studio}\\[-1pt]\scriptsize GUI + chat writing interface};
  \node[phase=green]  (script)   at (8,7.55) {\textbf{\texttt{.aaps} program}\\[-1pt]\scriptsize readable workflow language};
  \node[phase=yellow] (parser)   at (12,7.55) {\textbf{Parser}\\[-1pt]\scriptsize deterministic structured IR};
  \node[phase=orange] (compiler) at (16,7.55) {\textbf{Agent compiler}\\[-1pt]\scriptsize resolve blocks, code, tools};
  \node[phase=pink]   (runtime)  at (20,7.55) {\textbf{Runtime}\\[-1pt]\scriptsize execute, validate, recover};
  \node[phase=violet] (outputs)  at (24,7.55) {\textbf{Outputs}\\[-1pt]\scriptsize masks, overlays, tables, reports};

  \foreach \a/\b in {intent/studio,studio/script,script/parser,parser/compiler,compiler/runtime,runtime/outputs}
    \draw[arrow] (\a) -- (\b);

  \node[labeltext] at (2,8.03) {top-down design};
  \node[labeltext] at (6,8.03) {natural-language authoring};
  \node[labeltext] at (10,8.03) {syntax to structure};
  \node[labeltext] at (14,8.03) {unresolved to runnable};
  \node[labeltext] at (18,8.03) {plan to execution};
  \node[labeltext] at (22,8.03) {artifact trail};

  % Harness bands.
  \node[detail=blue] (design) at (1.9,5.55) {
    \textbf{Top-down experimental design}\\
    \scriptsize
    Expert-friendly overview first: biological goal, samples, imaging protocol,
    expected measurements, priors, constraints, and acceptable failure modes.
  };

  \node[detail=green] (contracts) at (7.45,5.55) {
    \textbf{Functional block contracts}\\
    \scriptsize
    Each block declares typed inputs and outputs, parameters, environment,
    tools, agents, actions, artifacts, validation rules, recovery, and tests.
  };

  \node[detail=orange] (compile) at (13,5.55) {
    \textbf{Agent-based compilation}\\
    \scriptsize
    Missing blocks, scripts, binaries, dependencies, tool registry entries,
    and agent prompts are resolved, generated, or reported before execution.
  };

  \node[detail=pink] (execution) at (18.55,5.55) {
    \textbf{Auditable execution}\\
    \scriptsize
    The runtime creates run folders, logs commands, tracks artifacts, checks
    validations, retries or falls back, and records repair provenance.
  };

  \node[detail=violet] (recordsdetail) at (24.1,5.55) {
    \textbf{Research record}\\
    \scriptsize
    The final outputs preserve the path from scientific question to code,
    execution logs, artifact versions, human approvals, and conclusions.
  };

  \draw[arrow] (script.south) to[out=-90,in=90] (contracts.north);
  \draw[arrow] (parser.south) to[out=-90,in=90] (compile.north);
  \draw[arrow] (runtime.south) to[out=-90,in=90] (execution.north);
  \draw[arrow] (outputs.south) to[out=-90,in=90] (recordsdetail.north);

  % Block contract decomposition.
  \node[smallbox=green] (inout) at (2.65,2.95) {
    \textbf{Data contract}\\
    \scriptsize inputs, outputs, optionality, defaults, paths, schemas
  };
  \node[smallbox=cyan] (actions) at (7.5,2.95) {
    \textbf{Action contract}\\
    \scriptsize Python, shell, internal tools, model calls, manual review
  };
  \node[smallbox=yellow] (tools) at (12.35,2.95) {
    \textbf{Capability contract}\\
    \scriptsize environment, packages, binaries, tools, models, agents
  };
  \node[smallbox=orange] (guards) at (17.2,2.95) {
    \textbf{Guardrail contract}\\
    \scriptsize validation, QC thresholds, safety settings, retry, fallback
  };
  \node[smallbox=pink] (records) at (22.05,2.95) {
    \textbf{Record contract}\\
    \scriptsize artifacts, logs, reports, repair prompts, provenance
  };

  \begin{scope}[on background layer]
    \node[harness=green, fit=(inout)(actions)(tools)(guards)(records)] (blockfit) {};
  \end{scope}
  \node[labeltext] at (12.35,4.05) {Block-level harness: divide-and-conquer unit of scientific automation};

  \foreach \a/\b in {inout/actions,actions/tools,tools/guards,guards/records}
    \draw[arrow] (\a) -- (\b);

  % Agent roles.
  \node[agent=violet] (writer) at (4,0.72) {
    \textbf{Writer agent}\\[-1pt]
    \scriptsize turns natural-language goals into structured workflows and reusable blocks
  };
  \node[agent=orange] (compileragent) at (12,0.72) {
    \textbf{Compiler agent}\\[-1pt]
    \scriptsize writes or repairs code, scripts, setup prompts, and tool bindings
  };
  \node[agent=pink] (runtimeagent) at (20,0.72) {
    \textbf{Runtime agents}\\[-1pt]
    \scriptsize handle irregular metadata, QC interpretation, method routing, and repair
  };

  \draw[feedback=violet] (writer.north) to[out=90,in=-90] (studio.south);
  \draw[feedback=orange] (compileragent.north) to[out=90,in=-90] (compiler.south);
  \draw[feedback=pink] (runtimeagent.north) to[out=90,in=-90] (runtime.south);

  % Outer harness boundary.
  \begin{scope}[on background layer]
    \node[harness=blue, fit=(intent)(outputs)(design)(recordsdetail)(blockfit)(writer)(compileragent)(runtimeagent)] {};
  \end{scope}
  \node[labeltext, text width=16cm] at (12,-0.28)
    {AAPS harness: make agent behavior inspectable, editable, reproducible, executable, and recoverable.};

  % Feedback loops.
  \draw[feedback=orange] (execution.south) to[out=-92,in=35] (compileragent.east);
  \node[labeltext, text width=3.8cm] at (15.7,0.86) {failure logs become repair prompts};

  % Example use case strip.
  \node[note, text width=23.5cm] at (12,-1.33) {
    \textbf{Example: organoid image analysis.}
    A scientist specifies the experiment and desired measurements first. AAPS
    expresses QC, segmentation, quantification, visualization, and reporting as
    typed blocks. The compiler resolves scripts and tools such as thresholding,
    Cellpose, or manual review. Runtime agents route low-quality or irregular
    data, while validation and human checkpoints keep masks, tables, and
    conclusions auditable.
  };
\end{tikzpicture}
\end{document}
